# Dev-mode backend URL — used to bypass the Vite proxy for large file uploads.
# Uploads go directly to Express instead of going through the Vite dev proxy,
# which has known issues buffering/stalling large multipart/form-data requests.
VITE_BACKEND_URL=http://localhost:3000
