# ============================================================
# LLM API Fast — Python Dependencies
# ============================================================

# --------------- Web Framework ---------------
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
sse-starlette>=1.6.0        # Server-Sent Events for streaming responses

# --------------- HTTP Client ---------------
httpx>=0.25.0               # Async HTTP client for llama.cpp backend

# --------------- Authentication ---------------
passlib[bcrypt]>=1.7.4      # Password hashing
python-jose[cryptography]>=3.3.0  # JWT tokens

# --------------- File Locking ---------------
filelock>=3.12.0            # Safe concurrent log/DB writes

# --------------- RAG: Vector Search ---------------
numpy>=1.24.0
sentence-transformers>=2.2.0  # Embedding model (bge-m3 etc.)
rank-bm25>=0.2.2            # BM25 for hybrid retrieval
faiss-cpu>=1.7.4            # Vector index (swap for faiss-gpu on CUDA systems)
# faiss-gpu>=1.7.4          # Uncomment for GPU-accelerated FAISS

# --------------- RAG: Document Parsing ---------------
pandas>=2.0.0               # CSV / Excel ingestion
openpyxl>=3.1.0             # Excel (.xlsx) backend for pandas
python-docx>=0.8.11         # DOCX parsing
PyMuPDF>=1.23.0             # Fast PDF text extraction (fitz)
langchain-community>=0.0.10 # Fallback PyPDFLoader for complex PDFs

# --------------- Image Handling ---------------
Pillow>=10.0.0              # Image resize / base64 encode for file uploads

# --------------- Web Search ---------------
tavily-python>=0.3.0        # Tavily API client for websearch tool

# --------------- PyTorch (GPU) ---------------
# torch is only required when RAG_EMBEDDING_DEVICE="cuda".
# Install the CUDA-matched build from https://pytorch.org/get-started/locally/
# Example (CUDA 12.1):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Leave this commented if running CPU-only or if torch is pre-installed.
# torch>=2.0.0
