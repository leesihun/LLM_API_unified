# ============================================================
#  AIhoonbot.com - Settings
#  Used by: start-all.sh, setup-linux.sh, close-apps.sh
# ============================================================

# --- Workspace ---
# Directory that Claude/Cursor will operate on
WORKSPACE_DIR="/scratch0"

# --- Port Settings ---
MESSENGER_PORT=10006
CLAUDE_WRAPPER_PORT=8000

# --- Cloudflare Tunnel ---
# Set to "true" to enable external access via aihoonbot.com
# Set to "false" for internal network only
USE_CLOUDFLARE=false
CLOUDFLARE_TUNNEL_NAME=aihoonbot
CLOUDFLARED_BIN="$HOME/.cloudflared/cloudflared"
CLOUDFLARED_CONFIG="$HOME/.cloudflared/config.yml"

# --- OpenCode ---
# OpenCode runs inside the Messenger server (no separate process).
# Accessible at http://localhost:$MESSENGER_PORT/opencode
# Set to "true" to warn if the opencode CLI binary is not found.
CHECK_OPENCODE=true
OPENCODE_CMD=opencode

# --- LLM API ---
LLM_API_PORT=10007
LLM_API_TOOLS_PORT=10007
# Optional: explicit LLM API base URL for remote server (overrides localhost/Cloudflare logic)
# Example: LLM_API_URL="http://192.168.0.50:10007"
# LLM_API_URL=""
# LLM API login credentials used by setup.py to obtain an API token
HOONBOT_LLM_USERNAME=admin
HOONBOT_LLM_PASSWORD=administrator

# --- Hoonbot ---
HOONBOT_PORT=3939
HOONBOT_BOT_NAME=Bot
HOONBOT_HOME_ROOM_ID=1
HOONBOT_HEARTBEAT_ENABLED=true
HOONBOT_HEARTBEAT_INTERVAL=360
# If heartbeat hits LLM connectivity errors, pause heartbeat LLM calls for this duration (seconds).
HOONBOT_HEARTBEAT_LLM_COOLDOWN_SECONDS=600
# Active hours: heartbeat only runs between these times (HH:MM 24h). Default = always on.
HOONBOT_HEARTBEAT_ACTIVE_START=00:00
HOONBOT_HEARTBEAT_ACTIVE_END=23:59

# --- Hoonbot Behavior ---
# Debounce window for rapid messages (seconds). Messages within this window are combined.
HOONBOT_DEBOUNCE_SECONDS=1.5
# LLM request timeout (seconds). Increase if tool-heavy calls timeout.
HOONBOT_LLM_TIMEOUT=300
# Max startup retry attempts for Messenger registration/webhook setup
HOONBOT_STARTUP_RETRIES=6
# Base delay between startup retries (seconds, doubles each attempt)
HOONBOT_STARTUP_RETRY_DELAY=1.0
# How many recent messages to scan per room on startup catch-up
HOONBOT_CATCHUP_LIMIT=20
# Max message length before auto-splitting
HOONBOT_MAX_MESSAGE_LENGTH=2000
# Session max age in days. Sessions older than this start fresh. 0 = never expire.
HOONBOT_SESSION_MAX_AGE_DAYS=7
# Enable streaming responses (shows tool status updates in real time)
HOONBOT_STREAMING=true
