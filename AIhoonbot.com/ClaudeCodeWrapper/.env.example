# Access token for authenticating from your phone
SECRET_TOKEN=changeme

# Server binding
HOST=0.0.0.0
PORT=8000

# CLI commands (adjust if your install path differs)
CLAUDE_CMD=claude
CURSOR_CMD=agent

# The project directory that Claude/Cursor will operate on
WORKSPACE_DIR=C:\Users\Lee\Desktop\Huni\MyProject

# Minimum seconds to wait between consecutive tasks (rate-limit bypass)
MIN_TASK_GAP_SECONDS=0

# Max seconds a single task can run before being killed
TASK_TIMEOUT_SECONDS=1800

# Cloudflare Tunnel — exposes the server to the public internet
# Requires cloudflared installed: https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/
TUNNEL_ENABLED=false
CLOUDFLARED_CMD=cloudflared
